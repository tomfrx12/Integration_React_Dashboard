const ButtonSideBar = ({texte}) => {
    return (
        <a className="px-6 py-3 hover:bg-[var(--color-blue)] flex cursor-pointer">
            <img src="" alt="" />
            <p>{texte}</p>
        </a>
    );
};

export default ButtonSideBar;